<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="managerlogin.css">
    <title>Manager</title>
</head>
<body>
    <header>
        <h3 id="btnNav">
            STOCK EASE
        </h3>
        <form action="logout" method="POST">
            <button type="submit" class="btn btn-danger logout">Logout</button>
        </form>
    </header>
    <nav class="navi">
        <div class="navi__links">
            <a href="mwarehouse" class="navi__link">
                <i class="material-icons">warehouse</i>
                Warehouse
            </a>
            <a  class="navi__link navi__link--active" href="mrestock">
                <i class="material-icons">add_business</i>
                Restock
            </a>
            <a href="mshipment" class="navi__link">
                <i class="material-icons">shopping_cart</i>
                Shipment
            </a>
            <a href="mproducts" class="navi__link">
                <i class="material-icons">inventory</i>
                Products
            </a>
            <a href="mainmanager" class="navi__link">
                <i class="material-icons">account_circle</i>
                Profile
            </a>
        </div>
    </nav>
    <form action="checkAdd" method="post">
    <div class="a1">
        <h2>Product Type</h2>
        <div class="input-group mb-3">
            <select name="ptype" class="form-select" id="inputGroupSelect02">
                <option selected>Choose...</option>
                <% if(type.length) { %>
                    <% for( i = 0 ; i < type.length ; i++) { %>
                        <option name="ptype" value="<%=type[i].type%>"><%=type[i].type %></option>
                    <% } %>
                <% } %>
            </select>
        </div>
    </div>
    <div class="a2">
        <h2>Product Name</h2>
        <div class="input-group mb-3">
            <select name="pname" class="form-select" id="inputGroupSelect02">
                <option selected>Choose...</option>
                <% if(name.length > 0) { %>
                    <% for(var i = 0 ; i < name.length ; i++) { %>
                        <option name="pname" value="<%=name[i].name%>"><%= name[i].name %></option>
                    <% } %>
                <% } %>
            </select>
        </div>
    </div>
    <div class="a3">
        <h2>Quantity</h2>
        <div class="input-group mb-3">
            <input type="number" class="form-control" placeholder="Number of products" name="pnum">
        </div>
    </div>
    <div class="butt">
        <button type="submit"class="btn btn-primary"><span></span>ADD TO WAREHOUSE</button>
    </div>
    </form>
    <% if(stockMSG == 'Success' || stockMSG == 'Failure') { %>
    <% if(num > 0) { %>
        <h5><%=num%> items couldnt be added due to no availability of sections for <%=ttype%></h5>
    <% } else if(num == 0) { %>
        <h5>items have been Added Successfully! </h5>
    <% } else if(stockMSG == 'Failure') {%>
        <h5>No Section available to add items! Enquire with the admin to add Section for <%= ttype%></h5>
    <% } else {console.log('exception')} %>
    <% } else { %>
        <% console.log('I have no Idea!!'); %>
    <% } %>
    <div class="modal fade" id="staticBackdrop" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
           <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title" id="staticBackdropLabel">Restock Status Message</h5>
              <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <% if(num > 0) { %>
                    <h5><%=num%> items couldnt be added due to no availability of sections for <%=ttype%></h5>
                <% } else if(num == 0) { %>
                    <h5>items have been Added Successfully! </h5>
                <% } else if(stockMSG == 'Failure') {%>
                    <h5>No Section available to add items! Enquire with the admin to add Section <%= ttype%></h5>
                <% } else {} %>
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
              <button type="submit" class="btn btn-primary">Add</button>
            </div>
          </div>
        </div>
    </div>
    <% if(stockMSG != '') { %>
        <script type="text/javascript" src="http://code.jquery.com/jquery-1.11.3.min.js">
        var $ = jQuery;
            $(function () {
                $("#staticBackdrop").modal('show');
            })
        </script>
    <% } %>
</body>
</html>