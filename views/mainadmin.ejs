<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <link rel="stylesheet" href="homeadmin.css">
        <title>Admin: Stock-Ease</title>
    </head>
    <body>
        <div class="wrap">
            <div class="bg"></div>
            <div class="bg1"></div>
            <div class="content">
                <header>
                    <h3 id="btnNav">
                        STOCK EASE
                    </h3>
                    <form action="logout" method="POST">
                        <button type="submit" class="btn btn-danger logout">Logout</button>
                    </form>
                </header>
                <nav class="navi">
                    <div class="navi__links">
                        <a href="logsadmin" class="navi__link">
                            <i class="material-icons">description</i>
                            Logs
                        </a>
                        <a  class="navi__link" href="secadmin">
                            <i class="material-icons">dns</i>
                            Sections
                        </a>
                        <a href="madmin" class="navi__link">
                            <i class="material-icons">people</i>
                            Manager
                        </a>
                        <a href="mainadmin" class="navi__link navi__link--active">
                            <i class="material-icons">person</i>
                            Profile
                        </a>
                        <a href="verifymadmin" class="navi__link">
                            <i class="material-icons">verified</i>
                            Manager Authentication
                        </a>
                    </div>
                    <div class="navi__overlay"></div>
                </nav>
                <!-- from here------------------------------------------------- -->
                <div class="adminprofile">
                    <h1>ADMIN INFORMATION</h1>
                    <% if(wresult.id) { %>
                        <h6>Admin id : <%= wresult.id%></h6>
                        <h6>username : <%= wresult.username%></h6>
                        <h6>Email Id : <%= wresult.email_id%></h6>
                    <% } %>
                    <h1>WAREHOUSE INFORMATION</h1>
                    <% if(result.id) { %>
                        <h6>Warehouse Id: <%= result.id %></h6>
                        <h6>Location: <%= result.location %></h6>
                    <% } %>
                </div>
            </div>
        </div>
    </body>
</html>
